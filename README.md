# postgres-tutorial
postgres tutorial

see: https://mebee.info/2020/12/04/post-24686/

procedure:

```
$ docker-compose up -d
$ docker volume ls
$ docker-compose ps
$ docker-compose down --rmi all --volumes --remove-orphans
$ docker volume ls
```
